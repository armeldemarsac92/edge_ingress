---
- name: Phase 5 - configure Teleport SSH certificate access on the edge VPS
  hosts: edge_vps
  gather_facts: true
  become: true
  roles:
    - role: vps_teleport
      tags: [teleport, ssh-access]
    - role: vps_edge_firewall
      tags: [firewall]
    - role: vps_edge_proxy
      tags: [proxy]
