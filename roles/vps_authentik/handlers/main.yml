---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  when: not ansible_check_mode

- name: Restart Authentik stack
  ansible.builtin.systemd:
    name: authentik-compose.service
    state: restarted
  when: not ansible_check_mode
