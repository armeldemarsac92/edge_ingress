---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  when: not ansible_check_mode

- name: Restart VPS edge firewall
  ansible.builtin.systemd:
    name: "{{ edge_firewall_service_name }}"
    state: restarted
    daemon_reload: true
  when: not ansible_check_mode
